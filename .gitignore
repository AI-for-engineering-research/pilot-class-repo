# macOS
.DS_Store
**/.DS_Store
._*
.AppleDouble
.LSOverride

# Obsidian vault config
.obsidian/

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini

# Build output (if a static site gets generated here later)
_site/
.quarto/
node_modules/

# Session 1 — commit the deck, plan, and the compressed making-of.mp4,
# but never the raw multi-hundred-MB screen capture or temp frame dumps.
session-1/assets/*.mov
**/_frames/
